/** Base Entity type to provide the base funcionality to all the entities of the project */
export type BaseEntity = {
  /** Sort Key of the item. Generally used as the identifier of the object. */
  id: string
}
